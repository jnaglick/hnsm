{
  "name": "hnsm",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "bootstrap": "npm install && lerna bootstrap",
    "build": "lerna run build --parallel",
    "build:container": "docker-compose run --rm builder",
    "clean": "rimraf **/dist && rimraf **/node_modules",
    "install:ci": "npm ci --no-optional && lerna run install:ci --parallel",
    "lint": "prettier . && eslint .",
    "lint:write": "prettier --write . && eslint --fix .",
    "start:dev": "lerna run start:dev --parallel",
    "test": "jest --config=./jest.config.js --verbose"
  },
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "27.0.3",
    "@typescript-eslint/eslint-plugin": "5.3.0",
    "@typescript-eslint/parser": "5.3.0",
    "eslint": "8.2.0",
    "jest": "27.4.4",
    "lerna": "^4.0.0",
    "prettier": "2.4.1",
    "rimraf": "3.0.2",
    "ts-jest": "27.1.1",
    "typescript": "4.4.4"
  }
}
